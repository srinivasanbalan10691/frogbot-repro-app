- params:
    git:
      repoName: frogbot-repro-app
      branches:
        - ${{ env.CURRENT_BRANCH }}

    scan:
      projects:
        - workingDirs:
            - "sc-backend"
          installCommand: "npm install"
          pathExclusions:
            - "**/node_modules/**"
